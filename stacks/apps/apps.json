{
  "_generated": "GENERATED FILE — do not edit by hand. This is the Nix-facing contract of the daedalus app registry (stacks/daedalus). The authoritative copy lives in daedalus’s `apps` table; this file is exported from it by the Apply flow, committed, and read by declarations.nix. Editing it here will be overwritten on the next Apply, and daedalus will show the app as drifted until then.",
  "_why": "Nix eval is pure and a flake only sees git-tracked files, so nixos-rebuild cannot query Postgres. Exporting to a committed file is what keeps `the repo IS the system` true: a fresh checkout rebuilds this exact box with no database in the loop.",
  "schemaVersion": 1,
  "apps": {
    "anansi": {
      "stage": "live",
      "postgres": true,
      "storage": false,
      "litellm": false,
      "prometheus": false,
      "operatorSecrets": false,
      "image": null,
      "hostname": null,
      "egress": null,
      "env": [],
      "auth": {
        "mode": "native",
        "healthPath": "/api/healthz"
      },
      "presentation": {
        "description": "Anansi — task-tracking experiment",
        "icon": "mdi-spider-#f59e0b"
      },
      "resources": {
        "cpus": null,
        "memoryMb": null,
        "pids": null
      },
      "notes": {
        "auth": "Native OIDC: anansi has its own accounts + per-user data, so only it can map a Pocket ID identity onto the right rows. The platform provides the client (id `anansi`, secret from clients.sops) and the OIDC_* env; the app repo owns the Auth.js provider."
      }
    },
    "ipcrawl": {
      "stage": "lab",
      "postgres": false,
      "storage": true,
      "litellm": false,
      "prometheus": false,
      "operatorSecrets": true,
      "image": null,
      "hostname": null,
      "egress": {
        "container": "gluetun-ipcrawl",
        "hostPort": 3100
      },
      "env": [
        {
          "key": "NUXT_ENABLE_LIVE_PROBE",
          "value": "true",
          "note": "ON: probe cameras live so cards show current frames instead of the static Shodan still. This dials arbitrary exposed cameras straight from the house's IP — enabled deliberately."
        },
        {
          "key": "NUXT_ENABLE_LIVE_FRAME_PERSIST",
          "value": "true",
          "note": "Write successful live frames back into the screenshot store, so the cached still stays fresh for when probing is off/unreachable."
        },
        {
          "key": "NUXT_SHODAN_LIMIT_PER_QUERY",
          "value": "1500",
          "note": "Cams pulled per Shodan query. ~1 credit / 100 results / query, so this is the main lever on credit burn. Upstream prod runs 2999."
        },
        {
          "key": "NUXT_OFFLINE_FOR_NOW",
          "value": "false",
          "note": "Maintenance brake, held off. \"true\" 503s the APIs and redirects HTML to /offline-for-now."
        },
        {
          "key": "NUXT_SHODAN_REFRESH_ON_BOOT",
          "value": "false",
          "note": "OFF: the Shodan pull runs on a Nitro in-process cron (daily 00:00), not at boot. On-boot refresh only fires when the DB is empty, but keeping it off means a fresh install waits for the cron or a manual trigger rather than pulling during first boot."
        },
        {
          "key": "NUXT_PUBLIC_SITE_URL",
          "value": "https://ipcrawl.toscanini.me",
          "note": "The app reads its own canonical origin from this, not from the APP_PUBLIC_URL the apps module injects."
        }
      ],
      "auth": {
        "mode": "proxy",
        "healthPath": "/favicon.ico"
      },
      "presentation": {
        "description": "ipcrawl — exposed-webcam catalogue",
        "icon": "mdi-cctv-#38bdf8"
      },
      "resources": {
        "cpus": null,
        "memoryMb": null,
        "pids": null
      },
      "notes": {
        "app": "Fork of github.com/alectrocute/ipcrawl (MIT). Upstream has no Dockerfile and deploys as systemd+nginx on a VPS; the fork adds the Dockerfile + .github/workflows/image.yml that publishes registry.toscanini.me/ipcrawl:latest, so the `image` default applies unchanged. Rebase the fork on upstream to pick up changes.",
        "auth": "Forward-auth: ipcrawl has no user model, so the gate belongs in front of it rather than inside it. No app-side change, and no identity headers — nothing downstream would read them. /favicon.ico is the bypass: harmless unauthenticated, and it comes from the app itself, so gatus and the deploy check certify the upstream instead of the middleware's 302.",
        "stage": "LAN-only. This is a catalogue of other people's exposed webcams; publishing it on a CNAME under our own domain is a decision to make deliberately, not a default.",
        "egress": "All outbound (camera live-probes + the daily Shodan pull) exits through the dedicated ProtonVPN tunnel in stacks/ipcrawl-vpn/ instead of the house WAN IP. The app leaves traefik-net and rides gluetun-ipcrawl's netns; traefik reaches the UI via the host port gluetun publishes.",
        "secrets": "NUXT_SHODAN_API_KEY + NUXT_VOTER_PEPPER + NUXT_CAM_ID_PEPPER come from the tracked stacks/apps/ipcrawl-env.sops (operatorSecrets). Without a Shodan key the app still boots and serves; the catalogue is just empty. The machine-generated secrets/ipcrawl/env keeps only the bootstrap AUTH_SECRET.",
        "storage": "SQLite + an fs-backed screenshot/SWR cache, no Postgres. Schema is created in-process on boot (server/utils/exploreDb.ts ensureSchema), so there's no migration step to run — the migrations/ dir upstream is for their Cloudflare D1 path, not this one. NUXT_SQLITE_PATH is baked into the image (=/app/data/explore.sqlite, i.e. inside the storage bind mount) — deliberately not restated here, so there's one source of truth for it."
      }
    },
    "voyra": {
      "stage": "lab",
      "postgres": true,
      "storage": false,
      "litellm": true,
      "prometheus": false,
      "operatorSecrets": true,
      "image": null,
      "hostname": null,
      "egress": null,
      "env": [],
      "auth": {
        "mode": "none"
      },
      "presentation": {
        "description": "Voyra app",
        "icon": "mdi-cube-outline-#94a3b8"
      },
      "resources": {
        "cpus": null,
        "memoryMb": null,
        "pids": null
      },
      "notes": {}
    }
  }
}
